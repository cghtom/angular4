import { EqualPwdDirective } from './equal-pwd.directive';

describe('EqualPwdDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualPwdDirective();
    expect(directive).toBeTruthy();
  });
});
